import { ToolPageLayout } from '@/components/tool-page-layout';

export default function PdfToImagePage() {
  return (
    <ToolPageLayout
      title="PDF to Image"
      description="Convert each page of your PDF into a separate high-quality JPG or PNG image. Perfect for sharing or embedding content."
      buttonText="Convert to Image"
    />
  );
}
