import { ToolPageLayout } from '@/components/tool-page-layout';

export default function SecurePdfPage() {
  return (
    <ToolPageLayout
      title="Secure PDF"
      description="Protect your PDF files by adding a password. You can also set permissions to restrict printing, copying, and editing."
      buttonText="Secure PDF"
    />
  );
}
