import { ToolPageLayout } from '@/components/tool-page-layout';

export default function CompressPdfPage() {
  return (
    <ToolPageLayout
      title="Compress PDF"
      description="Reduce the file size of your PDFs to make them easier to share and store, while maintaining the best possible quality."
      buttonText="Compress PDF"
    />
  );
}
